
model {

  # Priors
   tau <- pow(sd, -2)
   sd ~ dunif(0,1000)

  for (k in 1:2) {
    beta0[k]~ dnorm(0, 1e-6)
    beta1[k] ~ dnorm(0, 1e-6)

  }

  # Likelihood
  for (i in 1:n) {
     centroide[i] ~ dnorm(mu[i], tau)
    mu[i] <- beta0[sp[i]] + beta1[sp[i]] * vv[i]
    
    #1 cv
    #2 cm
    }
  }

    
